services:
  - type: web
    name: thedoctor-ml-service
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn app:app --host 0.0.0.0 --port $PORT
    # Optional: set any environment variables here via Render UI or envVars